# Timer

Timer und Wecker. Läuft als eigener Dienst, über eine dauerhafte Verbindung an Core
angebunden.

Anders als die übrigen Komponenten gibt es den Timer-Dienst nur als Docker-Image — kein
natives `install.sh`.
