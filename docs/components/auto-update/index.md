# Auto-Update

Hält die anderen Komponenten automatisch aktuell. Heißt intern noch "AutoDeploy",
obwohl "Auto-Update" die Funktion eigentlich besser trifft — der Name wird langfristig
noch überarbeitet.

Anders als die übrigen Komponenten läuft dieser Dienst als root statt als `hannah`-User.
