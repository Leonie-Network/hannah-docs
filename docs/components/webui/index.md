# WebUI

**Pflicht.** Neben Core die einzige zwingend erforderliche Komponente, da die meisten
Einstellungen ausschließlich hierüber verwaltet werden.

Web-Oberfläche zur Steuerung und Konfiguration von Hannah — läuft als eigener,
separater Dienst, in einem eigenen Repository entwickelt.
